# === SGLang Commander Backend ===
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.1.3
pydantic>=2.0.0
pydantic-settings>=2.0.0
email-validator>=2.0.0
httpx>=0.28.0
aiohttp>=3.14.0
psutil>=7.1.0
requests>=2.32.0
huggingface_hub>=0.27.0
semver>=3.0.0
websockets>=14.0
python-multipart>=0.0.18

# === SGLang (optional, only if you want to launch models locally) ===
# Uncomment to install sglang for local model serving
# Note: sglang is heavy (~5GB) and requires CUDA GPU
# sglang>=0.3.0
# transformers<4.56
# kernels<0.10

# === Compatible versions for sglang + transformers + kernels ===
# Run this if you get "No module named 'sglang'" or kernels errors:
# pip install "transformers<4.56" "kernels<0.10" "huggingface_hub>=0.27.0"

# === Desktop GUI (optional) ===
# PySide6>=6.8.0
# pyqtgraph>=0.13.0
# pglive>=0.4.0
# pynvml>=11.5.0

# === Development ===
alembic>=1.14.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
